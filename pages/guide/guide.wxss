/* 新手引导 */
.guide {
  height: 100vh;
  background: var(--bg);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 60rpx;
  box-sizing: border-box;
}
.skip {
  align-self: flex-end;
  margin-top: 40rpx;
  color: var(--ink-2);
  font-size: 26rpx;
}
.swiper {
  width: 100%;
  flex: 1 1 auto;
  min-height: 480rpx; /* 兜底：防止 flex 高度塌缩时整片空白 */
}
.slide {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 0 20rpx;
}
.seal {
  width: 200rpx; height: 200rpx;
  border-radius: 32rpx;
  background: var(--ph-accent, #2f8f78);
  color: #fff;
  font-size: 120rpx; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
  font-family: "Songti SC", "STSong", serif;
  margin-bottom: 48rpx;
  transform: rotate(-6deg);
  box-shadow: 0 12rpx 32rpx rgba(47,143,120,.28);
}
.title { font-size: 44rpx; font-weight: 700; color: var(--ink); margin-bottom: 28rpx; }
.desc { font-size: 30rpx; color: var(--ink-2); line-height: 1.7; max-width: 520rpx; }

.dots { display: flex; gap: 16rpx; margin-bottom: 40rpx; }
.dot { width: 14rpx; height: 14rpx; border-radius: 999rpx; background: var(--line); transition: all .2s; }
.dot.on { width: 36rpx; background: var(--c-poetry); }

.next {
  width: 100%;
  margin-bottom: 60rpx;
  background: var(--c-poetry);
  color: #fff;
}
